<div class="nav">
    <ul class="align--right">
        {{#foreach navigation}}
            <li class="nav-{{slug}}{{#if current}} nav-current{{/if}} display--inline" role="presentation"><a href="{{url absolute="true"}}" class="btn">{{label}}</a></li>
        {{/foreach}}
    </ul>
</div>
